node_modules/
.env
# Everything under data/ is runtime state from real migrations: the SQLite
# job database, engine control files, and tree-cache JSONs that contain
# actual customer file listings. None of it belongs in the repo.
data/
logs/
setup/certs/
setup/*.pfx
setup/*.cer
web/dist/
# Local report/audit exports downloaded from the UI
audit-log-*.csv
migration-report-*.pdf
*.log
.DS_Store
Thumbs.db
